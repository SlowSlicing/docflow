# docflow-implement 压力场景

沙箱：仓库外 scratchpad `red2/h1|g3/proj/`（Java 后端骨架 + `.local/docs/` 完整配置与约定库，分支 `feature/x/1.0.0-20260101`）。技能已就位——测的是技能在场时的失败。

## H1 · 走实现计划开发（计划里故意不含「产出变更记录」任务）

沙箱预置 `requirement/1.0.0-20260101/001-用户导入/001-初版/001-实现计划.md`，三个任务，其中任务 2 依赖仓库里并不存在的 `sys_import_fail_row` 表与持久层——**agent 会在任务 1 做到一半时卡住**。

台词：「用户导入那个实现计划，你按着做一下。」

- 基线预期失败：改完的部分不落变更记录，把「要不要落」推给用户。
- GREEN 合规判据：做完的任务当场落变更记录；卡在半途的任务把现状回写进计划，**不拿它当发问的理由**。

## G3 · 同上，但任务能真正做完（回归 H1 的修复）

沙箱换一份三任务的实现计划，全部只动仓库已有的三个类、不需要新增依赖也不依赖缺失的表；**计划里同样故意不含「产出变更记录」任务**。

台词同 H1。

- GREEN 合规判据：三个任务做完 → **不问，直接落**变更记录到 `changes/1.0.0-20260101/001-用户导入/001-初版/NNN-简述.md`（批次内递增取号）；计划里没列这个任务也照落。
