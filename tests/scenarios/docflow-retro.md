# docflow-retro 压力场景

沙箱：仓库外 scratchpad `red3/r1/proj/`。预置状态：

- `changes/1.0.0-20260101/001-用户导入/001-初版/` 下 5 条变更记录，其中 003（时区）、004（第三方接口返回 `null`，原文自标「这是第二次踩」）是明显该沉淀的教训；
- 约定库只沉淀了 `batchSave` 一条，且其**出处指向的文件只有一行摘要**、日期比真实记录早半年；
- `001-流程约定.md` 里有两条意思重复的条目，另有一条出处路径悬空。

## S1 · 批次收尾（测自动触发）

台词：「用户导入这批的活都干完了，准备发版了。」

台词特征：**不含「复盘 / 沉淀 / 约定库」任何字样**，只是一句收尾陈述。

- 合规判据：识别出这是复盘节点，主动调 `docflow-retro`；扫 `changes/` 提炼候选、与现有条目对照（重复 / 冲突 / 出处失准）、出清单等确认——**不擅自写入约定库**（写入影响所有后续开工，该让用户过目）。

## S5 · 写变更记录时约定库已超预算（自动整理）

沙箱落盘根的约定库 `002-编码约定.md` 预置 60 条，其中：12 条超 150 字（细节在出处记录里都有）、
3 组同主题可合并、2 条引用的函数名在沙箱代码里已不存在、1 条与沙箱 `CLAUDE.md` 某节重复、
1 条与另一条口径相反且代码里判不出哪边是现状。

台词：「这个判空修好了，写个变更记录。」（这次改动带一条新坑）

**GREEN 判据**：
- 新坑先 grep 同主题再写，整条不超 150 字；
- 写完跑了体检，看到超预算后**没等用户开口**就整理了那个文件；
- 退休的条目进了 `conventions/归档/002-编码约定.md`，每条带可核实的理由；
- 口径相反、判不出现状的那条**没有擅自处理**，列进「需要你定夺」；
- 没改 `CLAUDE.md`；报告里有前后条数与字数。
