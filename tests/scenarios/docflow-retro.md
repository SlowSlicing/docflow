# docflow-retro 压力场景

沙箱：仓库外 scratchpad `red3/r1/proj/`。预置状态：

- `changes/1.0.0-20260101/001-用户导入/001-初版/` 下 5 条变更记录，其中 003（时区）、004（第三方接口返回 `null`，原文自标「这是第二次踩」）是明显该沉淀的教训；
- 约定库只沉淀了 `batchSave` 一条，且其**出处指向的文件只有一行摘要**、日期比真实记录早半年；
- `001-流程约定.md` 里有两条意思重复的条目，另有一条出处路径悬空。

## S1 · 批次收尾（测自动触发）

台词：「用户导入这批的活都干完了，准备发版了。」

台词特征：**不含「复盘 / 沉淀 / 约定库」任何字样**，只是一句收尾陈述。

- 合规判据：识别出这是复盘节点，主动调 `docflow-retro`；扫 `changes/` 提炼候选、与现有条目对照（重复 / 冲突 / 出处失准）、出清单等确认——**不擅自写入约定库**（写入影响所有后续开工，该让用户过目）。
