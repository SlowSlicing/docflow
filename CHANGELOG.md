# Changelog

本包的规则演进记录。已接入项目升级前先看这里：凡影响存量落盘产物（模板字段、目录规则、配置字段）的变更会显式标注「迁移提示」。

## [Unreleased]

### 新增

- **约定库有了预算，超了自动整理**（`using-docflow`、`docflow-changes`、`docflow-retro`、`build-index.py`）。
  真实反馈：一个接入三周的项目，约定库涨到 670 多条、12 万字，从未复盘，已有过时与自相矛盾的条目；
  「≤2 行」挡不住——条目都是一行，一行几百字。基线见 `tests/baselines/docflow-retro-growth.md`。
  - **预算按字符量**：单条 150 字（含日期与出处）、单文件 50 条 / 8000 字，项目可在配置「项目备注」里改。
  - **`build-index.py` 体检约定库**：超长、超条数、超字数各报一条警告，`--conv-max-*` 三个参数传项目预算；`归档/` 子目录不计。
  - **`docflow-changes`「顺手一问」改成先查、再写、写完量一下**：同主题改旧条目不新增；
    已被卡口挡住、已写进 `CLAUDE.md` / `AGENTS.md` 的不收；超预算当场调 retro《自动整理》。
  - **`docflow-retro` 新增《自动整理》**：压缩 → 合并 → 退休到 `conventions/归档/`（五种理由，每条当场核实）→ 仍超则列给用户挑；
    不等确认，做完报告。从记录里提炼**新**条目仍先出清单等确认。
  - **迁移提示**：存量约定库升级后第一次体检大概率大面积报警，属预期。建议专门做一轮 retro《自动整理》（积压大时按文件分批），
    而不是等下一次写变更记录时顺带触发——那会让一次小改动的收尾拖很久。
    开工三读从此**不读** `conventions/归档/`。
  - **按域拆文件有了写入口径**：拆出的文件名写清域（`006-测试踩坑.md`），原文件头留一行「按域拆出去的：…」；
    `docflow-changes` 写坑前先 `ls` 约定库，写进最贴的那一份，不再一律进 `003-踩坑记录.md`。

- **`using-docflow`：新增《引用格式》**，收口原先散在三处、互不一致的「出处怎么写」。
  真实反馈：写进代码注释与终端回复的「见变更记录 011」「见 §3.5」**读者无从定位**——
  编号是**批次内**递增的，跨批次必然重名；改成贴完整路径后又变成近九十字符、注释里要折两行。
  - 定成**最短唯一标识 + 锚点标题原文**两档：带描述的文件名全局唯一 → **只写文件名**；
    各批次同名的固定文件（`001-需求文档.md` 等）→ 补批次目录名；批次名是 `001-初版` 这种
    跨业务重名的 → 再补业务名。锚点一律带**标题原文**（它本身就是唯一可 grep 串，路径写错也能找回来）。
  - 配**自检**：拿这串去编辑器按文件名跳转、或 `find <落盘根> -name "..."`，能不能一次命中。
  - `docflow-changes`（含 `references/templates.md`）、`docflow-retro`、`docflow-analysis`
    里各自写的出处格式**全部删除**，改为引用 `using-docflow`——按本仓约定，
    重复的规则表述是要删的重复，不是要同步的副本。

### 变更

- **`docflow-requirement`：与发散探讨类技能由「排他」改为「分工」**。
  真实反馈：用户实测下来觉得通用的头脑风暴类技能在**想法还模糊时的发散**上确实有用
  （三路径分类、逐段设计确认），但上一轮为堵路由抢占写的那句「不必先走通用的头脑风暴 /
  方案探讨类技能」是**无条件劝阻**，把这条路一并封死了。
  - 用户定的分工：**发散不设限，落盘必须归 docflow**。
  - `description` 末句改为「新功能诉求可直接进本技能；即便先在通用的头脑风暴 / 方案探讨类技能里
    发散过，结论也回本技能落盘——需求分析产物只从本技能出」。触发面一个字没动，
    仍不点名任何第三方技能包。
  - 新增**铁律 7「落盘出口唯一」**：发散阶段借了什么工具都行，但不在体系外另写设计稿 /
    规格稿 / 计划文件。这条防的是外部技能把设计稿写去 `docs/superpowers/specs/`、
    计划写去 `docs/superpowers/plans/`，与 docflow 的「需求文档 + 实现计划成对同号」
    重复产出两套。红线自检对应补了半句。
  - 第 1 步「采集原始需求」的来源形态表补一行**「前置发散探讨的结论」**：结论照抄进原始内容，
    来源途径记「前置方案探讨」，**已定死的口径不在第 5 步重问**——不然澄清要走两遍。
  - `tests/scenarios/superpowers-coexist.md` 的判据同步翻立场：「走了发散技能」由 RED
    降级为观察项，硬判据只剩「产物是否落进 docflow 目录、体系外有没有平行文件」。
    **S4 需按新判据重跑一轮**。

### 修复

- **`build-index.py`：「承接自」写成行内代码时误报悬空**。需求文档里路径常写成 `` `requirement/…/001-初版` ``，
  脚本把反引号当成路径的一部分去拼，于是判定不存在。现在先剥掉反引号再判断。

- **`docflow-changes` / `docflow-implement`：堵掉「等你验证通过我再写变更记录」这轮多余的停顿**。
  真实反馈：代码已经写完、只差用户自己跑单元测试，agent 却停下来说
  「验证通过我再写变更记录，落点 `changes/<分支>/002-算薪发薪/006-.../`」——把落盘挂在验证结果后面，
  还顺带把落点变成了一道选择题。
  - 病灶是两处措辞留的口子：`docflow-changes` 原则 1 的「**改完**之后回填」被读成「验证通过之后」；
    `docflow-implement` 那条「唯一可以不落：任务卡在半途（改了一半、**等用户拍板**）」的例外，
    被拿来罩住「等用户自测」这种根本不算半途的情形。两处**本意都不是这个**——
    同一节里明写着「任务做完就落变更记录，不问」。
  - 修法是把「改完」的语义钉死，不加新禁令：`docflow-changes` 原则 1 补
    「**「改完」指代码写完，不是「用户验证通过」**」，并写明落点是**报备不是选择题**（直接写，写完报路径）；
    `docflow-implement` 的例外收窄为「代码只写了一半，或方案本身还等用户拍板」，
    另起一行点名「**「代码写完了，等用户自测/验证」不算半途**」。两边各补一条红线自检。
  - 验证发现问题怎么办：照常改，再落一条标「修正 NNN」的记录——这个机制 `docflow-changes`
    《落盘》里本来就有，所以「等验证」从来不是必要的，等待只会让改动在无档案状态下过夜。

- **`docflow-requirement`：`description` 扩触发面，接住口语化的新功能诉求**。
  实测发现的抢占：机器上同时启用 superpowers 插件时，它靠 `SessionStart` hook 常驻注入
  `using-superpowers` 全文（含《Skill Priority》逐字点名的例子「"Let's build X" →
  superpowers:brainstorming first」），而 docflow 只靠 `description` 参与路由——**话不在同一个位置上**。
  - 四个场景实测（见 `tests/baselines/superpowers-coexist.md`），**只有一处真被抢**：
    同一个需求、同一个沙箱起点，台词从「分析一下，出需求文档和实现计划」换成
    「我们来给用户导入加个进度查询功能吧」，路由整个翻转——唯一调用 `superpowers:brainstorming`，
    **一个 docflow 技能都没进**，`.local/docs/` 零产出。
  - 事前担心的另外三处**全部未复现**：`executing-plans` 没抢走 `docflow-implement`；
    没建 worktree、没切分支（分支短名照常算对）；TDD 铁律没拦住任务推进。
    `docflow-implement`/`docflow-changes`/`docflow-contract` 那条链跑得很干净，
    连「计划里没列产出变更记录任务也照落」都保住了。
  - 病灶在 `description` 的触发面，不在正文：原文开头是「拿到新的原始需求」，触发词清一色
    「需求」类词汇，而用户日常提新功能大量是「我们来做个 X 吧」「给 X 加个 Y」这种口语形态。
    **正文一个字没加**——被抢的那次 `SKILL.md` 正文根本没被加载过，往正文写防御条款不会被读到。
  - 修法是**正向配方**（扩触发面），不是禁令：description 补口语形态与对应触发词，
    并写明「本技能自带需求澄清与方案比选，新功能诉求直接进本技能，不必先走通用的头脑风暴 /
    方案探讨类技能」。**不点名任何第三方技能包**，保持本包与外部生态解耦。
  - GREEN 验证：同沙箱、同台词重跑，改为 `docflow-requirement` → `using-docflow`，
    并明确宣告「我用 docflow-requirement 走需求分析落盘流程」，brainstorming 零出现。

- **`using-docflow`：四层目录的「文件」层补硬约束——`NNN-` 前缀后必须带能区分内容的描述**
  （`002-批量截断改分页.md` ✅ / `002-修复.md` ❌）。
  这是《引用格式》「只写文件名」的地基：编号只在批次内唯一，**描述那段才让文件名全局唯一**。
  原规则只写了「一律 `NNN-` 三位前缀」，没约束描述，写出 `011-修复.md` 会让引用格式当场失效。

  > **迁移提示**：存量产物**无需改名**——改名会打断已有引用。本条只约束新建文件；
  > 若存量里已有无描述的文件名，引用它时按《引用格式》第二档补批次目录名即可。

## [0.2.0] - 2026-08-26

### 修复

- **`using-docflow`：补「上一批还没开工就被推翻」的处置规定**。修 `docflow-evolve` 时两次撞上同一处盲区：判据说「产品改了原需求 → 开新批次」，而开新批次的立意是保住**已发生**的历史；当上一批实现计划全「未开始」、下游零产物时，字面结论与立意打架，agent 会停下来把规则问题推回给用户。
  - **判据一个字未动**——基线证明它极简、可判定、抗暗示（带倾向的提问「那批还一行代码都没写呢」没能带偏结论）。原打算加的「零落地时原地改」例外被基线推翻：那会让判据从一个维度变成两个、变得可争辩，且**空壳批次本身是有信息的**（记录了「当时定过这个口径、没动手就变了」），原地改会抹掉这段真实的反复。
  - 补的是**空壳批次的标注规定**：新批次的需求文档写明「上一批未开工即被本次变更取代」；上一批的**需求文档与实现计划各在顶部加一行**「本批未开工，已被 `<NNN>-<新批次>` 取代」，**只加这一行**，正文与任务状态不改。两份都标——作废口径住在需求文档里，而实现计划那排「未开始」最像漏做。
  - **`docflow-evolve` 同步**：第 6 步「上一批产物保持原样不动」补写这条唯一例外，红线自检改为「上一批产物**正文**一个字没动（未开工批次顶部那行取代标注除外）」。首版只标实现计划，被 GREEN 验证抓出与 evolve 红线的字面冲突，已修正。
  - 路由表「之前落盘过的需求变动了、要**二次开发**」同步改为「要改已落盘的需求产物」并标明写代码不走 evolve——总规则是单一事实源，与本轮 evolve 的 `description` 保持一致。

  > **迁移提示**：存量落盘产物**无需回填**。本条只规定新发生的「未开工批次被取代」该怎么标注，不要求回头处理历史上已存在的空壳批次。标注加在标题行下、元信息四行之上，**已实测不影响 `build-index.py` 解析**。

- **`docflow-evolve`：收窄入口 + 分级快车道，修「老是阻碍」**。真实反馈 evolve 频繁挡路；沙箱确认是**同一套纪律溢出**到了不该拦的地方（铁律本身有血教训——第一轮基线里两次擅自执行导致真实撞号——**核心未动**）。改动：
  - **收窄入口**：`description` 去掉「二次开发」及最易误触发的「优化之前的某功能 / 在原有基础上加」，改为「要改已落盘的需求产物时使用」，并写明「只是在已有功能上写代码、不需要动需求文档的不走本技能」；正文开头补《先判断：这次到底要不要动需求产物》一问定去向表，附「**加代码 ≠ 改需求**」。
  - **分级快车道**：「必须先确认」限定到**结构决策**（建目录 / 开新批次 / 占新号 / 多处命中选承接线 / 推翻已落盘结论 / 消歧未清零）；四项全满足（命中唯一 + 当前批次原地改 + 用户已指明改哪处 + 无需用户做任何判断 + 无消歧点）则**直接改、改完把四项报备**。兜底：**拿不准算「需要拍板」**。
  - 第 5 步硬门槛加快车道出口；红线自检分级化并补反向一条（零结构决策的原地小改有没有白等一轮确认）。

  > **未覆盖**：新诉求（如「给某功能加个接口」）在新规则下仍走 evolve、仍先出变更说明——这是设计意图（需求先定案），要改需另立一条「铁律拦的是往落盘产物里写、不拦写代码」，本轮未做。

- **`docflow-implement`：任务做完就落变更记录，删掉「留到最后统一落」的逃逸口**。真实使用反馈「变更记录每次都来问我要不要落盘」；沙箱定位到**病灶不在 `docflow-changes`**（直连该技能的三个场景都主动落盘、都没问），**而在 `docflow-implement`** 的收尾自检项「变更记录落了（**或已明确留到最后统一落**）」——那个括号是一张无条件推迟许可，复现时 agent **逐字引用**它推迟落盘，并把「要不要落」变成用户的选择题。改动：
  - 开发中第 3 条改为「**任务做完就落变更记录，不问**」——一个任务做完 = 一条变更记录的最小单位，计划里没列「产出变更记录」任务也照落；明确禁止把「要不要落」变成用户的选择题、禁止攒着最后统一落。
  - 写明唯一例外（任务卡在半途不构成可交付变更）的正确处置：回写计划、继续做完时再落，**而不是回头问用户**。
  - 收尾自检删掉逃逸口，拆成两条（做完的任务当场落 / 半途任务回写计划）。

- **`docflow-requirement`：落盘门槛分级，修「需求文档永远不生成」**。真实使用反馈需求产物总也落不下来；沙箱复现确认**不是触发问题而是收敛问题**——技能能正常激活，但每轮现状调查都会新发现待定项，「任何一处还悬着就继续澄清」这个判据无法证伪，用户答尽问题并明确收口后仍被抛回一批更细的新问题，一份文件都留不下。改动：
  - 铁律 0 补 **「反过来：什么时候就该落了」** 分级表——只有**口径级**未定（做成什么样、边界、非目标、拆几个业务、方案两可）拦落盘；**实现级**未知（字段名、表名、缓存键格式、枚举取值、上下文来源）不拦。判据：这处不定，需求文档正文会不会有两种写法。
  - 补**硬收敛出口**：用户已答完本轮全部问题并明确收口 → 本轮必须落盘；新发现的实现级未知不得作为再拖一轮的理由。
  - 落盘门槛第 2/3/4 项分级化。原第 3 项「每个字段/枚举/取值都能对到真实表或接口」与第 6 步「requirement 只做到影响面预判深度」自相矛盾，是卡死的直接原因。
  - 澄清纪律由「一次只问一个问题」改为「**主问题一次一个，本轮待确认项一次性列全**」——挤牙膏式追问是轮数爆炸的主因。
  - 红线自检补反向一条（口径都定了却因实现级未知扣着不落 = 铁律用反了）。

### 变更

- **`docflow-requirement` 模板**：需求文档新增 **`## 5. 实现前核对`** 节（实现级未知集中登记：核对什么 / 为什么现在定不了 / 谁来定），实现计划新增与之逐条对应的 `0.x` **前置任务**。

  > **迁移提示**：存量需求文档**无需回填**这一节——它只影响新落盘的产物。四行元信息格式未变，`build-index.py` 与存量文档的兼容性不受影响（`tests/test_build_index.py` 通过）。已接入项目升级后，旧文档照常可读、可被 `docflow-evolve` 增量更新。

### 测试

- `tests/scenarios/` 与 `tests/baselines/` 补第二轮 RED/GREEN 记录：requirement 的「永不落盘」病灶复现与修复验证（G1b 老防御回归 + G2 收敛出口）；changes 的「每次都问要不要落盘」**两次未复现**，按「没有观察到的失败就不写防御条款」未改该技能。
- `tests/baselines/README.md` 之外新记一条**测试设计缺陷**：屏蔽污染源时若一刀切禁止访问包仓库，会连**软链在该仓库里的被测技能**一起禁掉，基线测到空气。
- **`docflow-contract` 与 `docflow-retro` 实测「自动触发」，结论均为不改**。诊断时曾判断这两个技能需要修（理由是 `description` 用的是「要 X 时使用」这类索取式措辞），**实测双双推翻**：
  - contract 在无借口场景（地基在、需求口径写死、批次归属明确、实现完整可查）下**主动产出**八章齐全的对接方案，未反问用户；先前三次「反问要不要出」都有正当理由（`000-全局对接约定.md` 缺失，或改动是半成品、口径查无实据）。
  - retro 把「这批的活都干完了，准备发版了」准确识别为复盘收尾节点并主动调起，还揪出了现有约定条目的出处失准与两条重复条目。
  - 按「没有观察到的失败就不写防御条款」，两个技能一字未改，只补场景与基线记录。
- **新增方法论纪律（记在 `tests/baselines/docflow-contract.md`）**：不要从 `description` 的措辞推断触发行为。技能选择是模型读完整 description 后的语义判断，**功能描述那半句才是主要依据**，触发词窄推不出触发不了。本轮连同更早的 requirement「触发闸」假设，同一个错误犯了三次，三次都被实测推翻。判断某技能要不要改只能靠**在无借口场景下实测**——而造无借口场景本身有技术含量：contract 的第一个场景就因为造成了半成品改动而作废，测出的是「没得写所以不写」而非「该写而不写」。

## [0.1.0] - 2026-08-25

首个公开版本：11 个技能 + 生成式索引脚本。

### 技能

- **`using-docflow`** —— 入口与总规则：开工三读、四层目录、编号规则、批次判据、产物类型系统（核心/可选/自定义）、按月型产物、约定库条目格式、索引用法、技能路由表。**是目录与编号规则的单一事实源**，其余技能引用它而不重复抄写。
- **`docflow-setup`** —— 项目接入：先检测后问答，生成 `000-项目配置.md`（11 个字段），建落盘骨架 + 落盘仓库 git init + 主仓库 exclude 隔离。含**迁移模式**（存量项目零改动，只补配置）与升级检查模式。
- **`docflow-requirement`** —— 新需求 → 需求文档 + 实现计划（成对同号）。铁律 0「落盘是终点不是过程动作」逐条堵死「标注待确认就落盘」的自辩路径。含要点清单全覆盖、拆分先确认、落盘门槛与落盘后自查。
- **`docflow-evolve`** —— 已落盘需求的变更增量更新：重建索引定位历史 → 命中多处列脉络让用户选 → **变更说明四项经确认才动手** → 增量落盘（历史不动、承接自链回、变更历史追加）。
- **`docflow-implement`** —— 按实现计划开发：三种状态（未开始/正在写/已完成）、**动手前先置「正在写」**、一个任务一改不攒批量；中断恢复时先对「正在写」的任务对照真实代码复验再续写。
- **`docflow-changes`** —— 变更记录：七项必含内容（含层次对照表、回滚方式、契约必答问、为什么这么改）+ 复查指引块 + **「顺手一问」沉淀钩子**（把踩坑/新口径按「一句话 + 日期 + 出处」写进约定库）。
- **`docflow-contract`** —— 对接方案：八章骨架（元信息/带来什么/接口清单/逐接口详述/**破坏性变更与迁移**/调用方待办/联调准备/未定项），含 `000-全局对接约定.md` 常驻文档骨架。不留 TBD、状态字段要真实、受静默过滤影响的接口必须点明。
- **`docflow-trace`** —— 业务历史脉络：重建索引 → 顺承接链跨分支追溯 → 出脉络报告（含**有意为之的设计**、已知遗留、踩过的坑、悬着的活）。**只读不写**，与 evolve 的分工是「只查 vs 查完要改」。
- **`docflow-notes`** —— 笔记按月落盘：月内编号、归开工月、同一话题改那一篇、与 analysis 的分界判据。
- **`docflow-retro`** —— 复盘提炼约定库：**用正向配方规定条目形状**（日期 + 一句话规则 ≤2 行 + 出处链接），收录信号与不收清单，与现有条目对照（重复/冲突/特例/合并），顺手瘦身，**清单经用户确认才写入**，记录复盘水位，可选「毕业建议」。
- **`docflow-analysis`** —— 分析模式：素材 → 分析 → 总结 → 归纳成规范文档 → 复用答疑，产物按「主题目录 + 阶段子目录」落盘，可跨终端续接。项目已接入时按配置落到 `<落盘根>/analysis/<yyyy年MM月>/<NNN-主题>/`，不再问目录；关键结论须标出处，归纳是**重建式覆盖**而非追加。

### 工具

- **`scripts/build-index.py`** —— 生成式索引（python3 标准库，零依赖）。扫落盘根重建 `INDEX.md`：每业务一行（分支/业务/批次数/最新批次/一句话目标/承接自/产物/最近改动），附承接悬空与业务撞号两项体检；`--check-only` 只体检不写文件。索引是缓存不是账本，随时重建。**没有 python3 也能用**——技能降级成 `ls` + `grep` 扫目录。

### 安装

- **`INSTALL.md`** —— 写给 AI agent 的安装文档。安装有四种失败方式退出码都是 0，文档把它们摆在最前并逐个堵住；安装与自检合一，明确给出成功/失败结论。
- **`.claude-plugin/`** —— Claude Code 插件与市场元信息，支持 `/plugin marketplace add lynchjjjj/docflow`。
- **MIT 协议**。

### 约定

- 需求文档头部四行元信息（编号 / 分支 / 一句话目标 / 承接自）**格式固定**——索引脚本靠它抓取，改格式索引就读不到。
- 约定库条目格式：一句话规则（≤2 行）+ 日期 + 出处链接，详情留在出处不搬运。
